"""Modelos de la aplicación `usuarios`.

Por ahora no hay modelos definidos aquí. Si se necesita extender la
información del usuario (perfil, datos adicionales, etc.), se pueden
añadir modelos relacionados con `User` o un `UserProfile` en este archivo.
"""
from django.db import models

# Create your models here.
